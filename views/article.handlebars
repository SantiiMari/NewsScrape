<div class="jumbotron">
  <h1 class="display-4">{{article.title}}</h1>
        <p class="lead"> {{body}}</p>
</div>

<div class="row">
  <div class-"col s2"></div>
  <form action="/comment/{{artilce._id}}" method="POST" class="card-panel col s8">
  
  <h3>Leave your notes below</h3>
  <div class="row">
    <div class="input-field">
      <i class="material-icons prefix">perm_identity</i>
      <input id="icon+prefix" type="text" placeholder="Name" name="name" class="validate active">
    </div>
<div class="input-field">
      <i class="material-icons prefix">perm_identity</i>
      <input id="icon+prefix" type="text" placeholder="Name" name="name" class="validate active">
  </div>
  <div class="row">
    <div class="input-field">
      <center> <input class="btn add-comment-button topnav" type="submit" value="Submit"></center>
    </div>
  </div>
  </form>
</div>
<div class= "row">
  <div class="col s2"></div>
  <div class=:"col s8">
    <h3>Comments Below:</h3>
    {{#if article.comment}}
    {{#each article.comment}}
    <div class="card">
      <p id="nameComment"> {{name}}</p>
      <p>{{body}}</p>
    </div>
    {{/each}}
    {{else}}
    <h5>No notes taken</h5>
    {{/if}}
  </div>
</div>